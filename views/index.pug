//- doctype html
//- html
//-   head
//-     title Página Principal - Comercio Electrónico
//-     link(rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css")
//-   body
//-     .container.mt-5
//-       .row
//-         .col-md-12.text-center
//-           h1 Bienvenido a la Plataforma de Comercio Electrónico
//-           p.text-muted 
//-             | Aquí puedes explorar nuestra lista de productos, gestionarlos, 
//-             | o iniciar sesión para acceder a funcionalidades de administración.

//-       //- Sección de botones para navegar
//-       .d-flex.justify-content-center.mt-4
//-         a.btn.btn-primary.me-3(href="/productos") Ver Productos
//-         a.btn.btn-outline-primary(href="/login") Iniciar Sesión

//-       //- Imagen de fondo
//-       .mt-5.text-center
//-             img(src="/imagen.jpg", alt="Imagen de bienvenida", class="img-fluid")

//-     footer.mt-5.text-center
//-       p.text-muted &copy; 2024 Comercio Electrónico

doctype html
html
  head
    title Página Principal - Comercio Electrónico
    link(rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css")
    script(src="https://cdn.socket.io/4.0.0/socket.io.min.js") // Agrega Socket.io  
    script.  
      const socket = io(); // Conéctate a Socket.io  

      // Ejemplo de enviar un mensaje cuando se carga la página  
      document.addEventListener('DOMContentLoaded', () => {  
          const mensaje = 'Bienvenido a la aplicación de comercio electrónico';  
          socket.emit('mensaje', mensaje);  
      });  
      // Escucha mensajes del servidor  
      socket.on('mensaje', (data) => {  
          console.log('Mensaje recibido desde el servidor:', data);  
          
      });
  body
    .container.mt-5
      .row
        .col-md-12.text-center
          h1 Bienvenido a la Plataforma de Comercio Electrónico
          if user
            p.text-muted Bienvenido, #{user.name}! 👋
          else
            p.text-muted 
              | Aquí puedes explorar nuestra lista de productos, gestionarlos, 
              | o iniciar sesión para acceder a funcionalidades de administración.

      //- Sección de botones para navegar
      .d-flex.justify-content-center.mt-4
        a.btn.btn-primary.me-3(href="/productos") Ver Productos
        if user
          a.btn.btn-outline-danger(href="/logout") Cerrar Sesión
        else
          a.btn.btn-outline-primary(href="/login") Iniciar Sesión

      //- Imagen de fondo
      .mt-5.text-center
        img(src="/imagen.jpg", alt="Imagen de bienvenida", class="img-fluid")

    footer.mt-5.text-center
      p.text-muted &copy; 2024 Comercio Electrónico

