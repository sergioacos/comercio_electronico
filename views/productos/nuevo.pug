doctype html
html
  head
    title Crear Nuevo Producto
    link(rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css")
  body
    .container
      h1.mt-5 Crear Nuevo Producto
      form(action="/productos/nuevo" method="POST" class="mt-4")
        .mb-3
          label(for="nombre" class="form-label") Nombre del Producto
          input#nombre.form-control(type="text" name="nombre" placeholder="Nombre del producto" required)
        .mb-3
          label(for="precio" class="form-label") Precio
          input#precio.form-control(type="number" step="0.01" name="precio" placeholder="Precio del producto" required)
        .mb-3
          label(for="categoria" class="form-label") Categoría
          input#categoria.form-control(type="text" name="categoria" placeholder="Categoría del producto" required)
        .mb-3
          label(for="descripcion" class="form-label") Descripción
          textarea#descripcion.form-control(name="descripcion" rows="3" placeholder="Descripción del producto" required)
        .mb-3
          label(for="stock" class="form-label") Stock
          input#stock.form-control(type="number" name="stock" placeholder="Cantidad en stock" required)
        button.btn.btn-primary(type="submit") Crear Producto
      a.btn.btn-secondary.mt-3(href="/productos") Volver a la lista de productos